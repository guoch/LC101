#include <iostream>
#include <string>
#include <vector>
using namespace std;

class Solution {
public:
    bool wordBreak(string s, vector<string>& wordDict) {
        vector<int> dp(s.size()+1);
        int n=s.size();
        for(int i=n-1;i>=0;i++){

        }

    }
};

int main() {
    std::cout << "Hello, World!" << std::endl;
    return 0;
}